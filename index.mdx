---
title: "Selenite: Self-Hosted Web Proxy Powered by Ultraviolet"
sidebarTitle: "Introduction"
description: "Selenite is a deployable web proxy built on Ultraviolet and service workers. Bypass internet censorship and access blocked sites with a themeable, password-protected frontend."
---

Selenite is a self-hosted web proxy built on top of [Ultraviolet](https://github.com/titaniumnetwork-dev/Ultraviolet) by TitaniumNetwork. It runs entirely in the browser using service workers and an encrypted WebSocket transport, letting users browse the open web from behind restrictive networks — with no client-side software required.

Deploy it to any Node.js host in minutes, protect access with per-domain passwords, and customise the look with 16+ built-in themes.

<CardGroup cols={2}>
  <Card title="Quickstart" icon="bolt" href="/quickstart">
    Get Selenite running locally in under five minutes
  </Card>
  <Card title="Deployment" icon="cloud-arrow-up" href="/deployment">
    One-click deploys to Railway, Replit, Glitch, Koyeb, and Docker
  </Card>
  <Card title="Configuration" icon="sliders" href="/configuration/environment">
    Environment variables, passwords, and transport options
  </Card>
  <Card title="API Reference" icon="code" href="/reference/api">
    REST endpoints for managing domain access programmatically
  </Card>
</CardGroup>

## What Selenite Does

Selenite intercepts browser requests through a registered service worker and tunnels them via an encrypted [Wisp](https://github.com/MercuryWorkshop/wisp-protocol) WebSocket connection. The server rewrites URLs under the `/purple/service/` prefix so that the origin site never appears in the browser address bar and network-level filters cannot identify or block the traffic.

<CardGroup cols={3}>
  <Card title="Service Worker Proxy" icon="shield-halved">
    All traffic runs through a browser-registered service worker — no extensions or apps needed
  </Card>
  <Card title="Encrypted Transport" icon="lock">
    EpoxyTransport tunnels requests over an encrypted Wisp WebSocket connection
  </Card>
  <Card title="Password Protection" icon="key">
    Per-domain password gating keeps your instance private and access-controlled
  </Card>
  <Card title="16+ Themes" icon="palette">
    Switch between Selenite, Catppuccin, vaporwave, mountains, and more from the Settings page
  </Card>
  <Card title="One-Click Deploy" icon="rocket">
    Ready for Railway, Replit, Glitch, Koyeb, or any Docker-capable host
  </Card>
  <Card title="Open Blank Mode" icon="window-restore">
    Disguise the proxy in a separate tab while redirecting the original window
  </Card>
</CardGroup>

## Get Started

<Steps>
  <Step title="Clone and install">
    Clone the repository and install Node.js dependencies with npm.
  </Step>
  <Step title="Create a password file">
    Create `src/api/some.pass` with a JSON object mapping your domain to a password.
  </Step>
  <Step title="Set the environment variable">
    Copy `.env.example` to `.env` and set `ADD_LINK_PASS` for the management API.
  </Step>
  <Step title="Start the server">
    Run `npm start` and open `http://localhost:8080` in a Chromium browser.
  </Step>
</Steps>

<Note>
  Firefox requires a valid SSL certificate to run service workers. Use a Chromium-based browser when testing on `localhost`.
</Note>
